import { BG_ADMIN } from '../../assets';

export default function BgAdminDashboard() {
  return (
    <img
      src={BG_ADMIN}
      alt=""
      className="top-0 bottom-0 left-0 right-0 w-screen fixed object-fill z-0"
    />
  );
}
